#!/system/bin/sh
clear
path=$( cd "${0%/*}" && pwd -P )

chmod 755 $path/*


echo "######################################################################
Scripts principais instalados:
"

# loop da lista
find "$path" -maxdepth 1 -type f -name "*" ! -name "x?" | sort | while read fname; do
  basename "$fname"
done

